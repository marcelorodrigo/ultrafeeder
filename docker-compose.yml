services:
  ultrafeeder:
    image: ghcr.io/sdr-enthusiasts/docker-adsb-ultrafeeder:latest
    container_name: ultrafeeder
    hostname: ultrafeeder
    restart: unless-stopped
    device_cgroup_rules:
      - "c 189:* rwm"
    volumes:
      - /dev/bus/usb:/dev/bus/usb:rw
      - /opt/adsb/ultrafeeder/globe_history:/var/globe_history
      - /opt/adsb/ultrafeeder/graphs1090:/var/lib/collectd
      - /proc/diskstats:/proc/diskstats:ro
    tmpfs:
      - /run:exec,size=128M
    ports:
      - "8080:80"
    environment:
      TZ: ${FEEDER_TZ:-Europe/Amsterdam}
      READSB_DEVICE_TYPE: rtlsdr
      READSB_GAIN: ${READSB_GAIN:-auto}
      READSB_RTLSDR_DEVICE: ${ADSB_SDR_SERIAL:-1090}
      READSB_RTLSDR_PPM: ${ADSB_SDR_PPM:-0}
      READSB_LAT: ${FEEDER_LAT:?FEEDER_LAT is required}
      READSB_LON: ${FEEDER_LONG:?FEEDER_LONG is required}
      READSB_ALT: ${FEEDER_ALT_M:?FEEDER_ALT_M is required}m
      READSB_RX_LOCATION_ACCURACY: 2
      READSB_STATS_RANGE: "true"
      UUID: ${MULTIFEEDER_UUID:-}
      TAR1090_PAGETITLE: ${FEEDER_NAME:-ultrafeeder-pi3}
      ULTRAFEEDER_CONFIG: >-
        adsb,feed.airplanes.live,30004,beast_reduce_plus_out;
        adsb,feed.adsb.fi,30004,beast_reduce_plus_out;
        adsb,in.adsb.lol,30004,beast_reduce_plus_out;
        adsb,feed.planespotters.net,30004,beast_reduce_plus_out;
        adsb,feed.theairtraffic.com,30004,beast_reduce_plus_out;
        adsb,data.avdelphi.com,24999,beast_reduce_plus_out;
        adsb,dati.flyitalyadsb.com,4905,beast_reduce_plus_out;

  fr24:
    image: ghcr.io/sdr-enthusiasts/docker-flightradar24:latest
    container_name: fr24
    restart: unless-stopped
    depends_on:
      - ultrafeeder
    ports:
      - "8754:8754"
    environment:
      BEASTHOST: ultrafeeder
      FR24KEY: ${FR24_SHARING_KEY:-}
    tmpfs:
      - /var/log

  airnavradar:
    image: ghcr.io/sdr-enthusiasts/docker-airnavradar:latest
    container_name: airnavradar
    restart: unless-stopped
    depends_on:
      - ultrafeeder
    environment:
      BEASTHOST: ultrafeeder
      LAT: ${FEEDER_LAT}
      LONG: ${FEEDER_LONG}
      ALT: ${FEEDER_ALT_M}
      TZ: ${FEEDER_TZ:-Europe/Amsterdam}
      SHARING_KEY: ${AIRNAVRADAR_SHARING_KEY:-}
    tmpfs:
      - /var/log
